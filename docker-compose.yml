version: "3.9"

services:
  medpat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medpat-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped